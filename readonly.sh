# This is read only file.
# No one can edit it
# chattr +r readonly.sh #make it readonly -r for remove or =r to added readonly permission

# # find . -name "*.sh"  -mtime -1 #find how many files are modified within 1 day
# # grep -c "\.sh$" *.sh | wc -l # count num of files